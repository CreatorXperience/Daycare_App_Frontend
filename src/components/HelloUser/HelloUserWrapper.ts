import styled from "styled-components";

const HelloUserWrapper =styled.div`
    .hello-user-cont{
        width: 100%;
        display: flex;
        justify-content: space-between;
        padding: 10px;

        .hello-user-head{

         .greeting {
                font-size: 18px;
                font-weight: 400;
        }

        .user{
            font-size: 20px;
            font-weight: 800;
        }
        }
      
    }
`

export default HelloUserWrapper